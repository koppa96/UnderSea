export interface BuildingProps {
  id: number;
  imageUrl: string;
  title: string;
  description: string;
  amount: number;
  price: string;
}

export interface sendBuilding {
  id: number;
  count: number;
}
